import React from 'react';
import LoginForm from '../../components/LoginForm';

const Login: React.FC = () => {
  return (
    // Este contenedor básico asegura que el formulario esté centrado

      <LoginForm />

  );
};

export default Login;
